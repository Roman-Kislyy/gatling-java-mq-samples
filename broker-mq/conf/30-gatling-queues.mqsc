DEFINE QLOCAL ('GATLING.ORDERS.Q1.RQ') REPLACE
DEFINE QLOCAL ('GATLING.ORDERS.Q1.RS') REPLACE
DEFINE QLOCAL ('GATLING.TEST.Q1.RQ') REPLACE
DEFINE QLOCAL ('GATLING.TEST.Q1.RS') REPLACE

SET AUTHREC PROFILE ('GATLING.**') PRINCIPAL ('app') OBJTYPE(QUEUE) AUTHADD(BROWSE, GET, INQ, PUT, DSP)

* SET AUTHREC PROFILE ('SYSTEM.**') PRINCIPAL ('app') OBJTYPE(QUEUE) AUTHADD(BROWSE, GET, INQ, PUT, DSP)